---
description: 🎨 建立新元件流水線 (Component → Theme → Polish → i18n)
---

1. 使用 `/ui-component` 建立基礎結構。
2. 使用 `/ui-theme` 對齊設計系統語義。
3. 使用 `/ui-polish` 添加動效與互動。
4. 使用 `/ui-i18n` 處理多語系。
